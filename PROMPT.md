# 项目启动说明

本文件原有的 Hyperliquid × Lighter 分阶段计划已经失效，不再作为本项目的当前执行计划。

开始新任务时：

1. 先读取 `AGENTS.md` 和 `CLAUDE.md`。
2. 以用户当轮指令确定目标、范围和验收标准。
3. 只有在用户明确要求推进某个阶段时，才读取对应的专项计划，并在执行前确认该计划仍然有效。
4. 不要从本文件推断当前阶段，也不要自动恢复或继续已经结束、替换或废弃的计划。

历史计划如需追溯，请查看 Git 历史；不要将历史内容直接当作当前指令执行。
