# Shutdown review correction: finish the real lifecycle, not only the happy path

Continue your cleanup session in the same isolated fork. Astra reviewed your implementation and independently reviewed the architecture through a read-only subagent because shutdown cancellation/restart is a difficult case. The first implementation is NOT accepted yet. Read control/cleanup-review-findings.md and review/shutdown-review-1.md under E:/Nautilus-Perps/reports/ondo-acceptance/20260919-r52 (if the review file is still being written, the full findings below are authoritative as review hypotheses to verify).

Your first RED/GREEN runs remain valid evidence of the original missing hook. Preserve them and add a correction-round subdirectory; do not overwrite those records. Same scope/safety rules as the original task: only fork worktree and cleanup evidence; no .env, external private calls, orders, commits, installed wheel changes, or other task's files. Documentation and generated artifacts may be updated where the corrected code requires it. Diagnose each verified issue and add meaningful failing tests before fixing. Do not accept review claims blindly; counter-evidence is welcome.

## Verified blockers to reproduce and fix

1. A successful confirming GET is not a confirmed cancellation. confirm_cancel around execution.rs3197 clears uncertainty even if GET returns open/pending. The reconciliation background Found probe around reconciliation.rs3512 also clears cancel uncertainty for working orders. The stop may time out but release DMS because it only checks empty maps. Require actual terminal truth AND resolved fills/account state before confirming cancellation/clean stop/release. Include unresolved_orders and the settle/deadline outcome in the gate. Test accepted-without-order DELETE + GET open, background Found=open, and terminal canceled/filled with missing fills, all through the real lifecycle. Never release protection over unresolved work.
2. Read-only journal restore can populate tracked orders; the new unconditional stop loop sends DELETE despite account_read_only=true. Test a real journal produced by a prior run with owned open/unknown entries, reload read-only then disconnect, assert zero DELETE and zero DMS arm/release and retained unresolved state. Read-only must be side-effect free even with a nonempty ledger.
3. Request tasks are not drained/awaited by disconnect. Sync stop aborts but never finishes the TaskGroup generation, so reset->start_generation cannot resume (only finish_shutdown marks Drained). Trace existing TaskGroup APIs and patterns. Shutdown must stop admission, handle all already in-flight submissions/batches conservatively, terminate/await all owned tasks within a shared bound, preserve their unknown outcomes, and support an actual stop->reset->start->connect cycle. Do not create a second ownership system or merely call abort without waiting. Reentrant emitter worry was NOT confirmed: emitter uses mpsc; no need to refactor it.
4. stop() followed by disconnect() silently returns early because sync stop set disconnected and removed the transport. It must not lose unresolved work or report clean. Define and test graceful async cleanup versus synchronous emergency fallback. A fallback must register/checkpoint every unfinished owned write/order, preserve protection, and expose incomplete cleanup; subsequent disconnect/restart must not erase it. Repeated hooks must be idempotent and preserve the original outcome rather than success-by-early-return.
5. The five-second bound starts AFTER sequential DELETE/GET waits. Default HTTP timeout15s already exceeds LiveNode's default10s. Use one bounded shutdown deadline covering the whole operation, including cancel requests, confirmations, task joins and transport closing. Pre-register/checkpoint all unfinished work before awaits, so blocking the first of two owned orders cannot erase the second. Test an actual outer deadline canceling disconnect and subsequent sync stop/restart: both orders remain recoverable and all owned tasks/transports end. Avoid indefinite per-order multiplication of timeout.
6. disconnect converts known dirty StopReport into Ok, bypassing existing LiveNode error aggregation. Return an observable non-success with outstanding identities/reasons AFTER the bounded cleanup/persistence. Keep repeat calls interpretable. A log line alone does not meet the task's incomplete-result requirement.

## Acceptance tests

Drive the actual native factory/client hooks (and the real engine/kernel path if necessary to validate lifecycle ordering) for: clean normal stop; cancel/confirmation timeout; unknown submission through shutdown and restart; terminal missing fills; read-only restored journal; multiple owned orders with first request blocked; in-flight submit/batch shutdown; synchronous stop before disconnect; repeated disconnect; stop/reset/start/reconnect; external ownership. Existing direct stop_and_wait unit tests cannot replace these hook-level tests.

Keep protection fail-closed and preserve all existing meaningful tests. Stage completion requires both correct behavior and accurate result reporting. Full crate + Python feature tests/fmt are still required after corrections; preserve strict-warning/platform-exception distinction. App and wheel updates happen only after Astra accepts this correction. Report any truly necessary public binding/capability change; do not invent one just for tests.

Update cleanup/progress.md as you proceed, with cause/test/result. Finish with a corrected cleanup/README.md explicitly naming what the review caught, exact RED/GREEN evidence, total tests, limits, and how every user scenario reaches the actual client lifecycle.
